<UserControl x:Class="PIDetachSimulationInputWindow.Controls.SetInputView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PIDetachSimulationInputWindow.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="400">
    <Grid Margin="2 0 10 0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"
                              SharedSizeGroup="InOutIndexColumn"/>
            <ColumnDefinition Width="7*"/>
        </Grid.ColumnDefinitions>

        <Ellipse Width="15"
                 Height="15"
                 Fill="{Binding Value,Converter={StaticResource BooleanToFillOnOffConverter}}"
                 Stroke="LightGray"
                 StrokeThickness="1">
        </Ellipse>
        <TextBlock Grid.Column="1"
                   VerticalAlignment="Center"
                   HorizontalAlignment="Center"
                   Text="{Binding Id,StringFormat={}[{0}]}"
                   Margin="1 0"/>
        <Button Command="{Binding SetValueCommand}"
                Grid.Column="2"
                HorizontalContentAlignment="Center"
                VerticalContentAlignment="Center"
                Padding="3"
                MinWidth="80"
                MaxWidth="150">
            <TextBlock Text="{Binding Name}"
                       TextWrapping="Wrap"
                       TextAlignment="Center"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Center" />
        </Button>
    </Grid>
</UserControl>
